<nav class="navbar navbar-expand-lg fixed-top shadow-sm glass-navbar" data-bs-theme="light">
  <div class="container-fluid">

    <!-- Left: Logo -->
    <a class="navbar-brand text-primary d-flex align-items-center fw-bold me-2" href="/listing">
      <span class="material-symbols-outlined me-1">family_home</span>
      LivHaven
    </a>

    <!-- User buttons always visible on right -->
    <div class="d-flex align-items-center order-lg-3">
      <% if (!currentUser) { %>
        <a href="/signup" class="btn btn-outline-primary me-2 d-none d-md-inline-block">Sign Up</a>
        <a href="/login" class="btn btn-outline-primary d-none d-md-inline-block">Login</a>
      <% } else { %>
        <form action="/listing/new" method="GET" class="add-listing-form d-inline me-2">
          <button type="submit" class="btn btn-add-listing rounded-pill">
            <i class="fa-solid fa-plus me-2"></i> LivHaven Your Home
          </button>
        </form>
        <div class="dropdown d-none d-md-block">
          <button class="btn btn-sm btn-outline-secondary dropdown-toggle d-flex align-items-center"
            id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="fa-solid fa-user me-2"></i>
            <span class="d-none d-lg-inline"><%= currentUser.username %></span>
          </button>
          <div class="dropdown-menu dropdown-menu-end p-0 shadow-sm" aria-labelledby="userDropdown">
            <%- include('../includes/userCard.ejs') %>
          </div>
        </div>
        <a href="/logout" class="btn btn-outline-danger d-md-none ms-2">Logout</a>
      <% } %>
    </div>

    <!-- Toggler: collapses Explore & Search -->
    <button class="navbar-toggler order-lg-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
      aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible section -->
    <div class="collapse navbar-collapse justify-content-center order-lg-1" id="navbarCollapse">
      <ul class="navbar-nav mx-auto align-items-lg-center">
        <li class="nav-item me-lg-3">
          <a href="/listing" class="nav-link text-secondary">Explore</a>
        </li>
        <li class="nav-item w-100">
          <div class="country-search-container mt-2 mt-lg-0">
            <input type="text" id="country-input" class="form-control rounded-pill px-3 py-2"
              placeholder="Enter your country..." />
            <button type="button" class="btn btn-primary rounded-pill px-4 py-2 fw-semibold shadow-sm" id="nav-btn">
              LivHaven <i class="fa-solid fa-magnifying-glass-location"></i>
            </button>
          </div>
        </li>
      </ul>
    </div>

  </div>
</nav>
